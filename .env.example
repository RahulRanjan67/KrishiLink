# Required: long random value used to sign session cookies.
# Generate one with:
# python -c "import secrets; print(secrets.token_urlsafe(48))"
SESSION_SECRET=replace-with-a-long-random-secret

# Optional application settings.
BASE_PATH=/api
PORT=8080
